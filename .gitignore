# python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# node / frontend build
anvil/frontend/node_modules/
anvil/frontend/dist/

# anvil persistent memory is runtime state, not source
anvil/memory/rules.md
anvil/memory/failures.jsonl

# accumulated runtime data (build records, API logs, learned KB) is intentionally
# versioned — it's the app's growing history. See README "Data & learning".

# env
.env

# personal Claude Code session settings (keep launch.json, drop local perms)
.claude/settings.local.json
